services:
  - type: web
    name: truth-teller-backend
    runtime: python
    region: oregon
    plan: free

    rootDir: public/fake-news-backend

    buildCommand: pip install -r requirements.txt
    startCommand: python app.py

    envVars:
      - key: PYTHON_VERSION
        value: 3.10.8
